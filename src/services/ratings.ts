import { IRateProductQuery } from "../ts/services/ratings.types.ts"
import AxiosJSON from "../utils/helpers/axios.ts"

export async function rateProduct() {
  return { success: true };
}

export async function rateRetailer() {
  return { success: true };
}
