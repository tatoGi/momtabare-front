# Backend API URLs
# For development (local Laravel server)
VITE_BACKEND_URL=http://127.0.0.1:8000

# For production (your hosted domain)
# Option 1: Use specific production URL (recommended)
VITE_BACKEND_URL_PRODUCTION=https://system.momtabare.com

# Option 2: Alternative URLs for fallback (optional)
# VITE_BACKEND_URL_HTTPS=https://system.momtabare.com
# VITE_BACKEND_URL_HTTP=http://system.momtabare.com

# The app will automatically use:
# - VITE_BACKEND_URL for development
# - VITE_BACKEND_URL_PRODUCTION for production builds
# - Falls back to HTTPS/HTTP alternatives if available

# Default locale for API calls
VITE_DEFAULT_LOCALE=ka
